export { getStaticProps } from '../../getStaticProps'

import {
  component,
  dependencies,
  name,
  peerDependencies,
  version,
} from '@welcome-ui/loader/package.json'

<showcase
  version={version}
  name={name}
  component={component}
  description="Allows user to waiting information prefetch data or your page."
/>

## Usage

Predefined size `xs`, `sm`, `md`, `lg` or custom size. You can also change the color of loader.

```jsx column
<Loader size="xs" />
<Loader />
<Loader color="primary-40" size="md" />
<Loader color="primary-40" size="lg" />
<Loader color="primary-40" size="40px" />
<Loader color="primary-40" size={50} />
```

## Properties

<props propTypes={props.propTypes.Loader} />

## Packages

### Dependencies

<dependencies dependencies={dependencies} />

### Peer dependencies

<dependencies dependencies={peerDependencies} />

<pagination />
