export { getStaticProps } from '../../getStaticProps'

import {
  component,
  dependencies,
  name,
  peerDependencies,
  version,
} from '@welcome-ui/password-input/package.json'

# PasswordInput

<tagversion version={version} name={name} component={component} />

InputText component to create a password input with a show and hide password functionality.

## Install and import

<usage component={component} name={name} peerDependencies={peerDependencies} />

## Default

```jsx
<Form initialValues={{ password: 'qwerty' }}>
  <Field label="Password" required>
    <PasswordInput name="password" />
  </Field>
</Form>
```

## Properties

<props propTypes={props.propTypes.PasswordInput} />

## Dependencies

<dependencies dependencies={dependencies} />

## Peer dependencies

<dependencies dependencies={peerDependencies} />
