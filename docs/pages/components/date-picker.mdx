export { getStaticProps } from '../../getStaticProps'

import {
  component,
  dependencies,
  name,
  peerDependencies,
  version,
} from '@welcome-ui/date-picker/package.json'

# DatePicker

<tagversion version={version} name={name} component={component} />

It is based on the [react-datepicker](https://github.com/Hacker0x01/react-datepicker) library. _Note_: Pass a value of `null` if you don't want the default value of `Date.now()`.

## Install and import

<usage component={component} name={name} peerDependencies={peerDependencies} />

## Simple use case

```jsx
<Form initialValues={{ welcome: Date.now() }}>
  <Field label="Date">
    <DatePicker name="welcome" />
  </Field>
</Form>
```

## Pass props from the original library.

```jsx
<Form initialValues={{ welcome2: Date.now() }}>
  <Field label="Date">
    <DatePicker
      dateFormat="MMM dd yyyy"
      name="welcome2"
      maxDate={new Date()}
      popperProps={{ zIndex: 50 }}
      yearDropdownItemNumber={30}
    />
  </Field>
</Form>
```

## Give an Icon

```jsx
<Form initialValues={{ welcome3: Date.now() }}>
  <Field label="Date">
    <DatePicker icon={<DateIcon color="light.100" />} name="welcome3" />
  </Field>
</Form>
```

## fr locale

```jsx
<Form initialValues={{}}>
  <Field label="Date">
    <DatePicker locale={dateFR} name="welcome4" />
  </Field>
</Form>
```

## Properties

<props propTypes={props.propTypes.DatePicker} />

## Dependencies

<dependencies dependencies={dependencies} />

## Peer dependencies

<dependencies dependencies={peerDependencies} />
