export { getStaticProps } from '../../getStaticProps'

import {
  component,
  dependencies,
  name,
  peerDependencies,
  version,
} from '@welcome-ui/stack/package.json'

<Showcase
  version={version}
  name={name}
  component={component}
  description="Allows user to easily stack elements with space between them."
/>

## Usage

The default use of `Stack` will stack its children vertically.

```jsx
<Stack>
  <div>Foo</div>
  <div>Bar</div>
  <div>Baz</div>
</Stack>
```

You can change the `direction` & the `spacing`.

```jsx
<Stack direction="row" spacing="xl">
  <div>Foo</div>
  <div>Bar</div>
  <div>Baz</div>
</Stack>
```

It will ensure that its children are `li`s when is rendered as an `ul` or an `ol`.

```jsx
<Stack as="ul">
  <div>Foo</div>
  <div>Bar</div>
  <div>Baz</div>
</Stack>
```

## Properties

<Props propTypes={props.propTypes.Stack} />

## Packages

### Dependencies

<Dependencies dependencies={dependencies} />

### Peer dependencies

<Dependencies dependencies={peerDependencies} />

<Pagination />
