export { getStaticProps } from '../../getStaticProps'

import {
  component,
  dependencies,
  name,
  peerDependencies,
  version,
} from '@welcome-ui/radio-group/package.json'

<showcase
  version={version}
  name={name}
  component={component}
  description="Allows user to choose between some options and only one can be return."
/>

## Usage

```jsx
function () {
  const [value, setValue] = React.useState('github')

  return (
    <Field
      label="Label"
      required
    >
      <RadioGroup name="social-1" options={constants.ITEMS.slice(0, 5)} onChange={setValue} value={value} />
    </Field>
  )
}
```

## Row style

Add flexDirection to `row` to Field to add the inline style for radios group

```jsx
function () {
  const [value, setValue] = React.useState('github')

  return (
    <Field
      label="Label"
      required
      flexDirection="row"
    >
      <RadioGroup name="social-1" options={constants.ITEMS.slice(0, 5)} onChange={setValue} value={value} />
    </Field>
  )
}
```

## Disabled

Add `disabled` to the Field component

```jsx
function () {
  return (
    <Field
      label="Label"
      disabled
    >
      <RadioGroup name="social-2" options={constants.ITEMS.slice(0, 5)} value="github" />
    </Field>
  )
}
```

## Variants

Use `warning`, `error`, `info` or `success` properties on Field to add a variant status on your RadioGroup. The label, hint or border color are set by magic 🪄

```jsx
function () {
  const ITEMS = constants.ITEMS.slice(0, 3)

  return (
    <Field
      label="Label"
      error="an error"
    >
      <RadioGroup name="social-3" options={ITEMS} />
    </Field>
  )
}
```

## With hint on radio

An example with a hint on radio.

```jsx
function () {
  const [value, setValue] = React.useState(null)

  return (
    <Field required label="Label">
      <RadioGroup
        name="social-4"
        maxWidth={{ md: 0.3 }}
        options={[
          {
            value: 'behance',
            label: 'Behance',
            hint: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit.',
          },
          {
            value: 'dribbble',
            label: 'Dribbble',
          },
        ]}
        onChange={setValue}
        value={value}
      />
    </Field>
  )
}
```

## Properties

<props propTypes={props.propTypes.RadioGroup} />

## Packages

### Dependencies

<dependencies dependencies={dependencies} />

### Peer dependencies

<dependencies dependencies={peerDependencies} />

<pagination />
