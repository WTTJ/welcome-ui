export { getStaticProps } from '../../getStaticProps'

import {
  component,
  dependencies,
  name,
  peerDependencies,
  version,
} from '@welcome-ui/icons/package.json'

<showcase
  version={version}
  name={name}
  component={component}
  customInstall={'yarn add @welcome-ui/icons.avatar'}
  customUsage={`import { AvatarIcon } from '@welcome-ui/icons.avatar'`}
/>

## Size

Use size property with `xs`, `sm`, `md`, `lg` or `xl`.

```jsx row
<TwitterIcon size="xs" />
<TwitterIcon size="sm" />
<TwitterIcon />
<TwitterIcon size="lg" />
<TwitterIcon size="xl" />
```

## Colour

Icons inherit the colour from their parent.

```jsx row
<TwitterIcon color="blue" size="xl" />
<TwitterIcon color="orange" size="xl" />
<TwitterIcon color="green" size="xl" />
```

## Available icons

**Note**: You can click on an icon to copy its name.

### Arrows

```jsx withCode=false
function() {
  return IconsList({ name: 'arrows' })
}
```

### Actions

```jsx withCode=false
function() {
  return IconsList({ name: 'actions' })
}
```

### Miscellaneous

```jsx withCode=false
function() {
  return IconsList({ name: 'miscellaneous' })
}
```

### WTF

```jsx withCode=false
function() {
  return IconsList({ name: "wtf" })
}
```

### Files

```jsx withCode=false
function() {
  return IconsList({ name: 'files' })
}
```

### Player

```jsx withCode=false
function() {
  return IconsList({ name: 'player' })
}
```

### Avatar

```jsx withCode=false
function() {
  return IconsList({ name: 'avatar' })
}
```

### Markdown

```jsx withCode=false
function() {
  return IconsList({ name: 'markdown' })
}
```

### Table

```jsx withCode=false
function() {
  return IconsList({ name: 'table' })
}
```

### Brands

```jsx withCode=false
function() {
  return IconsList({ name: 'brands' })
}
```

### Flags

```jsx withCode=false
function() {
  return IconsList({ name: 'flags' })
}
```

## Properties

<props propTypes={props.propTypes.Icon} />

## Packages

### Dependencies

<dependencies dependencies={dependencies} />

### Peer dependencies

<dependencies dependencies={peerDependencies} />

<pagination />
