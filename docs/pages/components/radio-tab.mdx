export { getStaticProps } from '../../getStaticProps'

import {
  component,
  dependencies,
  name,
  peerDependencies,
  version,
} from '@welcome-ui/radio-tab/package.json'

# Radio Tab

<tagversion version={version} name={name} component={component} />

## Install and import

<usage component={component} name={name} peerDependencies={peerDependencies} />

## Usage

```jsx
<Form initialValues={{ social_1: 'twitter', social_2: 'xing' }}>
  <Field flexDirection="row" label="Social networks" mb="xl" required>
    <RadioGroup name="social_1" options={constants.ITEMS} renderOption={RadioTab} />
  </Field>
  <Field label="Social networks" mb="xl" required w={1 / 3}>
    <RadioGroup name="social_2" options={constants.ITEMS} renderOption={RadioTab} />
  </Field>
</Form>
```

## Properties

<props propTypes={props.propTypes.RadioTab} />

## Dependencies

<dependencies dependencies={dependencies} />

## Peer dependencies

<dependencies dependencies={peerDependencies} />
