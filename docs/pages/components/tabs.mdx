export { getStaticProps } from '../../getStaticProps'

import {
  component,
  dependencies,
  name,
  peerDependencies,
  version,
} from '@welcome-ui/tabs/package.json'

<showcase
  version={version}
  pageName="Tabs"
  name={name}
  component={component}
  description="Allows user to make it easy explore and switch between different views."
/>

## About

See [Ariakit](https://ariakit.org/components/tab) for documentation.

## Usage

Set the tab activated by default using `selectedId` on `useTab`.

```jsx
function() {
  const tab = useTab({ defaultSelectedId: 'tab1' })

  return (
    <>
      <Tab.List aria-label="Tabs" store={tab}>
        <Tab store={tab} id="tab1">
          Tab 1
        </Tab>
        <Tab store={tab} id="tab2">
          Tab 2
        </Tab>
        <Tab store={tab} id="tab3">
          Tab 3
        </Tab>
        <Tab store={tab} id="tab4">
          Tab 4
        </Tab>
        <Tab store={tab} disabled id="tab5">
          Tab 5
        </Tab>
      </Tab.List>
      <Tab.Panel store={tab} tabId="tab1">
        Tab.Panel 1
      </Tab.Panel>
      <Tab.Panel store={tab} tabId="tab2">
        Tab.Panel 2
      </Tab.Panel>
      <Tab.Panel store={tab} tabId="tab3">
        Tab.Panel 3
      </Tab.Panel>
      <Tab.Panel store={tab} tabId="tab4">
        Tab.Panel 4
      </Tab.Panel>
      <Tab.Panel store={tab} disabled tabId="tab5">
        Tab.Panel 5
      </Tab.Panel>
    </>
  )
}
```

## Sizes

Size is set on Tab.List. Available sizes are `sm` and `md`. By default size is set to `md`.

```jsx
function() {
  const tab = useTab({ defaultSelectedId: 'tab1' })

  return (
    <>
      <Tab.List aria-label="Tabs" store={tab}>
        <Tab store={tab} id="tab1">
          Tab 1
        </Tab>
        <Tab store={tab} id="tab2">
          Tab 2
        </Tab>
        <Tab store={tab} id="tab3">
          Tab 3
        </Tab>
      </Tab.List>
      <Tab.Panel store={tab} tabId="tab1">
        Tab.Panel 1
      </Tab.Panel>
      <Tab.Panel store={tab} tabId="tab2">
        Tab.Panel 2
      </Tab.Panel>
      <Tab.Panel store={tab} tabId="tab3">
        Tab.Panel 3
      </Tab.Panel>
      <br/>
      <Tab.List aria-label="Tabs" size="sm" store={tab}>
        <Tab store={tab} id="tab1">
          Tab 1
        </Tab>
        <Tab store={tab} id="tab2">
          Tab 2
        </Tab>
        <Tab store={tab} id="tab3">
          Tab 3
        </Tab>
      </Tab.List>
      <Tab.Panel store={tab} tabId="tab1">
        Tab.Panel 1
      </Tab.Panel>
      <Tab.Panel store={tab} tabId="tab2">
        Tab.Panel 2
      </Tab.Panel>
      <Tab.Panel store={tab} tabId="tab3">
        Tab.Panel 3
      </Tab.Panel>
    </>
  )
}
```

## With no border

The border on `Tab.List` is an inset shadow that you can customize or disable via the `boxShadow` style prop

```jsx
function() {
  const tab = useTab({ defaultSelectedId: 'tab1' })

  return (
    <>
      <Tab.List aria-label="Tabs" boxShadow="none" store={tab}>
        <Tab store={tab} id="tab1">
          Tab 1
        </Tab>
        <Tab store={tab} id="tab2">
          Tab 2
        </Tab>
        <Tab store={tab} id="tab3">
          Tab 3
        </Tab>
        <Tab store={tab} id="tab4">
          Tab 4
        </Tab>
        <Tab store={tab} disabled id="tab5">
          Tab 5
        </Tab>
      </Tab.List>
      <Tab.Panel store={tab} tabId="tab1">
        Tab.Panel 1
      </Tab.Panel>
      <Tab.Panel store={tab} tabId="tab2">
        Tab.Panel 2
      </Tab.Panel>
      <Tab.Panel store={tab} tabId="tab3">
        Tab.Panel 3
      </Tab.Panel>
      <Tab.Panel store={tab} tabId="tab4">
        Tab.Panel 4
      </Tab.Panel>
      <Tab.Panel store={tab} disabled tabId="tab5">
        Tab.Panel 5
      </Tab.Panel>
    </>
  )
}
```

## With a badge

Add badges, icons and images in tab.

```jsx
function() {
  const tab = useTab({ defaultSelectedId: 'tab2' })
  const selectedId = tab.useState('selectedId')

  const getVariant = item => (selectedId === item ? 'primary' : 'default')

  return (
    <>
      <Tab.List aria-label="Tabs" store={tab}>
        <Tab store={tab} id="tab1">
          Tab 1
          <Badge variant={getVariant('tab1')}>
            new
          </Badge>
        </Tab>
        <Tab store={tab} id="tab2">
          Tab 2
          <Badge variant={getVariant('tab2')}>
            old
          </Badge>
          <Badge shape="circle" variant={getVariant('tab2')}>
          2
          </Badge>
        </Tab>
        <Tab store={tab} id="tab3">
          Tab 3
          <HeartIcon />
        </Tab>
        <Tab store={tab} id="tab4">
          <img src="https://cdn.welcometothejungle.com/wttj-front/production/assets/images/logos/wttj.svg?v=3a3dbd7122a01600fb67e66b889bb47c" />
          Tab 4
        </Tab>
      </Tab.List>
      <Tab.Panel store={tab} tabId="tab1">
        Tab.Panel 1
      </Tab.Panel>
      <Tab.Panel store={tab} tabId="tab2">
        Tab.Panel 2
      </Tab.Panel>
      <Tab.Panel store={tab} tabId="tab3">
        Tab.Panel 3
      </Tab.Panel>
      <Tab.Panel store={tab} tabId="tab4">
        Tab.Panel 4
      </Tab.Panel>
    </>
  )
}
```

## Use another component

Use another component in tab.

```jsx
function() {
  const tab = useTab({ defaultSelectedId: 'tab1' })

  return (
    <>
      <Tab.List aria-label="Tabs" store={tab}>
        <Tab store={tab} as="a" id="tab1">
          Tab 1
        </Tab>
        <Tab store={tab} as="a" id="tab2">
          Tab 2
        </Tab>
        <Tab store={tab} as="a" id="tab3">
          Tab 3
        </Tab>
      </Tab.List>
      <Tab.Panel store={tab} tabId="tab1">
        Tab.Panel 1
      </Tab.Panel>
      <Tab.Panel store={tab} tabId="tab2">
        Tab.Panel 2
      </Tab.Panel>
      <Tab.Panel store={tab} tabId="tab3">
        Tab.Panel 3
      </Tab.Panel>
    </>
  )
}
```

## One tab

Active bar doesn't display with only one tab.

```jsx
function() {
  const tab = useTab({ defaultSelectedId: 'tab1' })

  return (
    <>
      <Tab.List aria-label="Tabs" store={tab}>
        <Tab store={tab} id="tab1">
          Tab 1
        </Tab>
      </Tab.List>
      <Tab.Panel store={tab} tabId="tab1">
        Tab.Panel 1
      </Tab.Panel>
    </>
  )
}
```

## Vertical

Set the tab orientation by providing the `orientation` prop to the tab state

```jsx
function() {
  const tab = useTab({ orientation: 'vertical', defaultSelectedId: 'tab2' })

  return (
    <Box display="flex">
      <Tab.List w={200} mr="lg" aria-label="Tabs" store={tab}>
        <Tab store={tab} id="tab1">
          Tab 1
        </Tab>
        <Tab store={tab} id="tab2">
          Tab 2
        </Tab>
        <Tab store={tab} id="tab3">
          Tab 3
        </Tab>
        <Tab store={tab} id="tab4">
          Tab 4
        </Tab>
      </Tab.List>
      <Tab.Panel store={tab} tabId="tab1">
        Tab.Panel 1
      </Tab.Panel>
      <Tab.Panel store={tab} tabId="tab2">
        Tab.Panel 2
      </Tab.Panel>
      <Tab.Panel store={tab} tabId="tab3">
        Tab.Panel 3
      </Tab.Panel>
      <Tab.Panel store={tab} tabId="tab4">
        Tab.Panel 4
      </Tab.Panel>
    </Box>
  )
}
```

## Properties

### Tab

<props propTypes={props.propTypes.Tabs} />

### Tab.List

<props propTypes={props.propTypes['Tabs.TabList']} />

### Tab.Panel

<props propTypes={props.propTypes['Tabs.TabPanel']} />

## Packages

### Dependencies

<dependencies dependencies={dependencies} />

### Peer dependencies

<dependencies dependencies={peerDependencies} />

<pagination />
