export { getStaticProps } from '../../getStaticProps'

import { Checkbox } from '@welcome-ui/checkbox'
import {
  component,
  dependencies,
  name,
  peerDependencies,
  version
} from '@welcome-ui/toggle/package.json'

# Toggle

<tagversion version={version} name={name} />

## Install and import

<usage component={component} name={name} peerDependencies={peerDependencies} />

## Examples

Use `checked` property

```jsx
function() {
  const [toggle, setToggle] = useState(false)
  const [toggleChecked, setToggleChecked] = useState(true)

  return (
    <>
      <Toggle mr="lg" checked={toggle} onClick={() => setToggle(!toggle)} />
      <Toggle checked={toggleChecked} onClick={() => setToggleChecked(!toggleChecked)} />
    </>
  )
}
```

## Disabled

With the `disabled` property

```jsx
function() {
  const [toggle, setToggle] = useState(false)
  const [toggleChecked, setToggleChecked] = useState(true)

  return (
    <>
      <Toggle mr="lg" checked={toggle} onClick={() => setToggle(!toggle)} disabled />
      <Toggle checked={toggleChecked} onClick={() => setToggleChecked(!toggleChecked)} disabled />
    </>
  )
}
```

## ConnectedField

Examples with `ConnectedField`

```jsx
<Form
  initialValues={{
    toggle: true,
    'toogle-checked': true,
    'toogle-disabled': true,
    'toggle-label': false,
    'disabled-label': true
  }}
>
  <Box display="flex" mb="lg">
    <ConnectedField name="toggle" component={Toggle} mr="lg" />
    <ConnectedField name="toggle-checked" component={Toggle} mr="lg" />
    <ConnectedField name="toggle-disabled" component={Toggle} disabled />
  </Box>
  <ConnectedField name="toggle-label" component={Toggle} label="Toggle with a label" mb="lg" />
  <ConnectedField name="disabled-label" component={Toggle} label="Disabled with a label" disabled />
</Form>
```

## Properties

<props of="Checkbox" propTypes={props.propTypes} />

## Dependencies

<dependencies dependencies={dependencies} />

## Peer dependencies

<dependencies dependencies={peerDependencies} />
