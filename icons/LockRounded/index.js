import React from 'react'
import { Icon } from '@welcome-ui/icon'

import content from './content.js'
export const LockRoundedIcon = props => (
  <Icon alt="LockRounded" content={content} {...props} />
)
