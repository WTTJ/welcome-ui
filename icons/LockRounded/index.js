import React from 'react'
import { Icon } from '@welcome-ui/icon'

import content from './content.json'

export function LockRoundedIcon(props) {
  return <Icon alt="LockRounded" content={content} {...props} />
}
