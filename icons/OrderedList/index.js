import React from 'react'
import { Icon } from '@welcome-ui/icon'

import content from './content.js'
export const OrderedListIcon = props => (
  <Icon alt="OrderedList" content={content} {...props} />
)
