import React from 'react'
import { Icon } from '@welcome-ui/icon'

import content from './content.js'
export const CandidateStatusIcon = props => (
  <Icon alt="CandidateStatus" content={content} {...props} />
)
