import React from 'react'
import { Icon } from '@welcome-ui/icon'

import content from './content.json'
export const PositiveOutlineIcon = props => (
  <Icon alt="PositiveOutline" content={content} {...props} />
)
