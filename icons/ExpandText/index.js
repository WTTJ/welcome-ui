import React from 'react'
import { Icon } from '@welcome-ui/icon'

import content from './content.js'
export const ExpandTextIcon = props => (
  <Icon alt="ExpandText" content={content} {...props} />
)
