import React from 'react'
import { Icon } from '@welcome-ui/icon'

import content from './content.js'
export const StackserverIcon = props => (
  <Icon alt="Stackserver" content={content} {...props} />
)
