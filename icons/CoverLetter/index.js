import React from 'react'
import { Icon } from '@welcome-ui/icon'

import content from './content.js'
export const CoverLetterIcon = props => (
  <Icon alt="CoverLetter" content={content} {...props} />
)
