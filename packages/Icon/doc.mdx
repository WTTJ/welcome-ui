---
name: Icon
route: /components/icon
menu: Components
---

import { Props, Playground } from 'docz'
import { Code, Dependencies, IconsList, TagVersion } from '../../docz'
import { TwitterIcon } from '../../icons/Twitter'
import { Icon } from './index'
const { dependencies, peerDependencies, name, version } = require('./package.json')

# Icon

<TagVersion version={version} name={name} />

## Install and import individual icons

<Code language="bash" isCopyable>{`yarn add @welcome-ui/icons.avatar ${Object.keys(peerDependencies).join(' ')}`}</Code>
<Code language="bash" isCopyable>{`import { AvatarIcon } from '@welcome-ui/icons.avatar'`}</Code>

## Or… install and import all icons

<Code language="bash" isCopyable>{`yarn add @welcome-ui/icons ${Object.keys(peerDependencies).join(' ')}`}</Code>
<Code language="bash" isCopyable>{`import * as Icons from '@welcome-ui/icons'`}</Code>

## Size

Use size property with `xs`, `sm`, `md`, `lg` or `xl`.

<Playground>
  <TwitterIcon size="xs" />
  <TwitterIcon size="sm" />
  <TwitterIcon />
  <TwitterIcon size="lg" />
  <TwitterIcon size="xl" />
</Playground>

## Colour

Icons inherit the colour from their parent

<Playground>
  <TwitterIcon color="blue" name="twitter" size="xl" />
  <TwitterIcon color="orange" name="twitter" size="xl" />
  <TwitterIcon color="green" name="twitter" size="xl" />
</Playground>

## Available icons

**Note**: You can click on an icon to copy its name.

### Products

<Playground wrapper={false}>
  {() => {
    const icons = ['ats', 'employerBranding', 'marketplace', 'welcomeHome', 'welcomeKit']
    return IconsList(icons)
  }}
</Playground>

### Directions

<Playground wrapper={false}>
  {() => {
    const icons = ['left', 'right', 'up', 'down']
    return IconsList(icons)
  }}
</Playground>

### Actions

<Playground wrapper={false}>
  {() => {
    const icons = [
      'activity',
      'add',
      'archive',
      'bulk',
      'check',
      'collapse',
      'cross',
      'dragAndDrop',
      'edit',
      'eye',
      'expand',
      'externalLink',
      'filters',
      'get',
      'heart',
      'heartOutline',
      'information',
      'link',
      'log_out',
      'menu',
      'more',
      'notification',
      'pin',
      'play',
      'playOutline',
      'print',
      'promote',
      'refuseCandidate',
      'remote',
      'reset',
      'search',
      'settings',
      'trash',
      'upload',
      'write'
    ]
    return IconsList(icons)
  }}
</Playground>

### Appreciations

<Playground wrapper={false}>
  {() => {
    const icons = [
      'negative',
      'positive',
      'negativeOutline',
      'positiveOutline',
      'negativeStraight',
      'positiveStraight'
    ]
    return IconsList(icons)
  }}
</Playground>

### Categories

<Playground wrapper={false}>
  {() => {
    const icons = ['actions', 'coverLetter', 'details', 'resume', 'questions']
    return IconsList(icons)
  }}
</Playground>

### Miscellaneous

<Playground wrapper={false}>
  {() => {
    const icons = [
      'address',
      'alert',
      'anniversary',
      'attachment',
      'avatar',
      'birthday',
      'candidateStatus',
      'celebrate',
      'comment',
      'commentOutline',
      'contract',
      'create',
      'date',
      'department',
      'earth',
      'educationLevel',
      'email',
      'emailOutline',
      'female',
      'information',
      'location',
      'male',
      'offerStatus',
      'origine',
      'phone',
      'phoneOutline',
      'podcast',
      'question',
      'review',
      'reviewOutline',
      'source',
      'star',
      'suitcase',
      'tag',
      'update'
    ]
    return IconsList(icons)
  }}
</Playground>

### Files

<Playground wrapper={false}>
  {() => {
    const icons = [
      'csv',
      'doc',
      'docx',
      'eot',
      'gif',
      'jpeg',
      'jpg',
      'pdf',
      'png',
      'svg',
      'ttf',
      'txt',
      'woff',
      'woff2',
      'xls',
      'xlsx',
      'zip'
    ]
    return IconsList(icons)
  }}
</Playground>

### WTF

<Playground wrapper={false}>
  {() => {
    const icons = [
      'basketball',
      'book',
      'bulb',
      'calcul',
      'camera',
      'chevron',
      'coffee',
      'compass',
      'computer',
      'crossTarget',
      'crown',
      'dish',
      'flag',
      'flagPointed',
      'gear',
      'handshake',
      'hardHat',
      'hashtag',
      'heartShiny',
      'lock',
      'lockRounded',
      'map',
      'megaphone',
      'mic',
      'microship',
      'money',
      'mountain',
      'music',
      'paperplane',
      'pencil',
      'piechart',
      'pizza',
      'shield',
      'spanner',
      'stackserver',
      'sun',
      'target',
      'thumbup',
      'tree'
    ]
    return IconsList(icons)
  }}
</Playground>

### Markdown

<Playground wrapper={false}>
  {() => {
    const icons = [
      'bold',
      'code',
      'heading1',
      'heading2',
      'italic',
      'link',
      'unorderedList',
      'orderedList',
      'quote',
      'strikethrough'
    ]
    return IconsList(icons)
  }}
</Playground>

### Social networks

<Playground wrapper={false}>
  {() => {
    const icons = [
      'behance',
      'dribbble',
      'facebook',
      'github',
      'instagram',
      'linkedin',
      'stackoverflow',
      'twitch',
      'twitter',
      'xing',
      'youtube'
    ]
    return IconsList(icons)
  }}
</Playground>

## Properties

<Props of={Icon} />

## Dependencies

<Dependencies dependencies={dependencies} />

## Peer dependencies

<Dependencies dependencies={peerDependencies} />
