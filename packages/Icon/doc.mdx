---
name: Icon
route: /components/icon
menu: Components
---

import { Props, Playground } from 'docz'
import { Code, IconsList, TagVersion } from '../../docz'
import { TwitterIcon } from '../../icons/Twitter'
import { Icon } from './index'
const { dependencies, peerDependencies, name, version } = require('./package.json')

# Icon

<TagVersion version={version} name={name} />

## Import

<Code>{`
  import { AvatarIcon, PlayIcon } from '@welcome-ui/icons'
  --OR--
  import * as Icons from '@welcome-ui/icons'
`}</Code>

## Size

Use size property with `xs`, `sm`, `md`, `lg` or `xl`.

<Playground>
  <TwitterIcon size="xs" />
  <TwitterIcon size="sm" />
  <TwitterIcon />
  <TwitterIcon size="lg" />
  <TwitterIcon size="xl" />
</Playground>

## Colour

Icons inherit the colour from their parent

<Playground>
  <TwitterIcon color="blue" name="twitter" size="xl" />
  <TwitterIcon color="orange" name="twitter" size="xl" />
  <TwitterIcon color="green" name="twitter" size="xl" />
</Playground>

## Available icons

### Products

<Playground wrapper={false}>
  {() => {
    const icons = ['ats', 'employerBranding', 'marketplace', 'welcomeHome', 'welcomeKit']
    return IconsList(icons)
  }}
</Playground>

### Directions

<Playground wrapper={false}>
  {() => {
    const icons = ['left', 'right', 'up', 'down']
    return IconsList(icons)
  }}
</Playground>

### Actions

<Playground wrapper={false}>
  {() => {
    const icons = [
      'add',
      'archive',
      'attachment',
      'bulk',
      'check',
      'collapse',
      'cross',
      'date',
      'expand',
      'externalLink',
      'filters',
      'heart',
      'heartOutline',
      'information',
      'link',
      'menu',
      'more',
      'pin',
      'play',
      'print',
      'refuseCandidate',
      'reset',
      'search',
      'settings',
      'trash',
      'write'
    ]
    return IconsList(icons)
  }}
</Playground>

### Appreciations

<Playground wrapper={false}>
  {() => {
    const icons = [
      'negativeOutline',
      'positiveOutline',
      'negative',
      'positive',
      'negativeStraight',
      'positiveStraight'
    ]
    return IconsList(icons)
  }}
</Playground>

### Categories

<Playground wrapper={false}>
  {() => {
    const icons = ['actions', 'coverLetter', 'details', 'resume', 'questions']
    return IconsList(icons)
  }}
</Playground>

### Miscellaneous

<Playground wrapper={false}>
  {() => {
    const icons = [
      'activity',
      'address',
      'avatar',
      'candidateStatus',
      'commentOutline',
      'comment',
      'crossTarget',
      'department',
      'emailOutline',
      'email',
      'language',
      'location',
      'offerStatus',
      'origine',
      'phoneOutline',
      'phone',
      'promote',
      'reviewOutline',
      'review',
      'source',
      'specialPipeline',
      'tag',
      'female',
      'male',
      'create',
      'update'
    ]
    return IconsList(icons)
  }}
</Playground>

### WTF

<Playground wrapper={false}>
  {() => {
    const icons = [
      'anniversary',
      'basketball',
      'birthday',
      'book',
      'bulb',
      'calcul',
      'camera',
      'celebrate',
      'chevron',
      'coffee',
      'compass',
      'computer',
      'crown',
      'dish',
      'flag',
      'gear',
      'handshake',
      'hardHat',
      'hashtag',
      'heartShiny',
      'lock',
      'map',
      'megaphone',
      'mic',
      'microship',
      'money',
      'mountain',
      'music',
      'paperplane',
      'pencil',
      'piechart',
      'pizza',
      'remote',
      'shield',
      'spanner',
      'stackserver',
      'sun',
      'target',
      'thumbup',
      'tree'
    ]
    return IconsList(icons)
  }}
</Playground>

### Markdown

<Playground wrapper={false}>
  {() => {
    const icons = [
      'bold',
      'code',
      'heading1',
      'heading2',
      'italic',
      'link',
      'unorderedList',
      'orderedList',
      'quote',
      'strikethrough'
    ]
    return IconsList(icons)
  }}
</Playground>

### Social networks

<Playground wrapper={false}>
  {() => {
    const icons = [
      'behance',
      'dribbble',
      'facebook',
      'github',
      'instagram',
      'linkedin',
      'stackoverflow',
      'twitter',
      'xing',
      'youtube'
    ]
    return IconsList(icons)
  }}
</Playground>

## Properties

<Props of={Icon} />

## Dependencies

<Dependencies dependencies={dependencies} />

## Peer dependencies

<Dependencies dependencies={peerDependencies} />
