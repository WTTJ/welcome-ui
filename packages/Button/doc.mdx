---
name: Button
route: /components/button
menu: Components
---

import { Props, Link as LinkDocz, Playground } from 'docz'
import { Button } from './index'
import { EarthIcon } from '../../icons/Earth'
import { SunIcon } from '../../icons/Sun'
import { Link } from '../Link'
import { Code, Dependencies, TagVersion, Usage } from '../../docz'
const { dependencies, peerDependencies, component, name, version } = require('./package.json')

# Button

<TagVersion version={version} name={name} />

Built with [Reakit](https://reakit.io/docs/button/) for a better accessibility 🥰

## Install and import

<Usage component={component} name={name} peerDependencies={peerDependencies} />

## Variants

### Basics

<Playground>
  <Button>Primary</Button>
  <Button variant="secondary">Secondary</Button>
  <Button variant="tertiary">Tertiary</Button>
  <Box backgroundColor="nude.700" display="inline" px="sm" py="xxl">
    <Button variant="tertiary-negative">Tertiary negative</Button>
  </Box>
  <Button variant="quaternary">Quaternary</Button>
</Playground>

### States

<Playground>
  <Button variant="primary-warning">Primary warning</Button>
  <Button variant="secondary-warning">Secondary warning</Button>
  <Button variant="primary-danger">Primary danger</Button>
  <Button variant="secondary-danger">Secondary danger</Button>
</Playground>

## Formats

Use size property with `xs` `sm` `md`(default) `lg` or `xl`.

<Playground>
  <Button size="xs">Tiny</Button>
  <Button size="sm">Small</Button>
  <Button>Medium</Button>
  <Button size="lg">Large</Button>
  <Button size="xl">XLarge</Button>
</Playground>

## Disabled

All disabled buttons have the same style.

<Playground>
  <Button variant="primary-danger" disabled>
    Disabled
  </Button>
</Playground>

## With icons

<Playground>
  <Button>
    <EarthIcon />
    <span>Button</span>
  </Button>
  <Button variant="secondary">
    <EarthIcon />
    <span>Button</span>
  </Button>
  <Button variant="tertiary">
    <EarthIcon />
    <span>Button</span>
  </Button>
  <Button disabled>
    <EarthIcon />
    <span>Button</span>
  </Button>
</Playground>

## Shape

Get same size on `width` and `height`, can have a 'circle' or 'square' `shape`.

<Playground>
  <Button shape="square" size="xs">
    <EarthIcon size="xs" />
  </Button>
  <Button shape="circle" size="xs">
    <EarthIcon size="xs" />
  </Button>
  <Button shape="square" size="sm">
    <EarthIcon size="sm" />
  </Button>
  <Button shape="circle" size="sm">
    <EarthIcon size="sm" />
  </Button>
  <Button shape="square">
    <EarthIcon />
  </Button>
  <Button shape="circle">
    <EarthIcon />
  </Button>
  <Button shape="square" size="lg">
    <EarthIcon size="lg" />
  </Button>
  <Button shape="circle" size="lg">
    <EarthIcon size="lg" />
  </Button>
  <Button shape="square" size="xl">
    <EarthIcon size="xl" />
  </Button>
  <Button shape="circle" size="xl">
    <EarthIcon size="xl" />
  </Button>
</Playground>

## Transform to a link

You can transform your Button component with `as` property to add you linker, `href` or `to`... all that you want.

<Playground>
  <Button as="a" href="http://www.welcometothejungle.co" target="_blank" rel="noopener nofollow">
    <SunIcon />
    <span>Welcome</span>
  </Button>
</Playground>

## Width

You can add `width` property from [xstyled](https://xstyled.dev/docs/style-props/).

<Playground>
  <Button width={1}>full width</Button>
  <Button width={1 / 2}>half width</Button>
</Playground>

## Radius

You can add `borderRadius` property from [xstyled](https://xstyled.dev/docs/style-props/).

<Playground>
  <Button>default</Button>
  <Button borderRadius="10px">10px</Button>
</Playground>

## Group Buttons

<div>
  See documentation from{' '}
  <Link as={LinkDocz} to="/components/group">
    Group
  </Link>{' '}
  component .
</div>

## Properties

<Props of={Button} />

## Dependencies

<Dependencies dependencies={dependencies} />

## Peer dependencies

<Dependencies dependencies={peerDependencies} />
