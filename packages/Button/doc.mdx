---
name: Button
route: /components/button
menu: Components
---

import { Props, Link, Playground } from 'docz'
import { Button, ButtonGroup } from './index'
import { LanguageIcon } from '../../icons/Language'
import { SunIcon } from '../../icons/Sun'
import { TagVersion } from '../../docz'
const { name, version } = require('./package.json')

# Button

<TagVersion version={version} name={name} />

Built with Reakit for a better accessibility 🥰

## Variants

### Basics

<Playground>
  <Button>primary (default)</Button>
  <Button variant="secondary">secondary</Button>
  <Button variant="tertiary">tertiary</Button>
</Playground>

### States

<Playground>
  <Button variant="primary-warning">primary-warning</Button>
  <Button variant="secondary-warning">secondary-warning</Button>
  <Button variant="primary-danger">primary-danger</Button>
  <Button variant="secondary-danger">secondary-danger</Button>
</Playground>

## Formats

Use size property with `xs` `sm` `md`(default) `lg` or `xl`.

<Playground>
  <Button size="xs">Tiny</Button>
  <Button size="sm">Small</Button>
  <Button>Medium</Button>
  <Button size="lg">Large</Button>
  <Button size="xl">XLarge</Button>
</Playground>

## Disabled

All disabled buttons have the same style.

<Playground>
  <Button variant="primary-danger" disabled>
    Disabled
  </Button>
</Playground>

## With icons

<Playground>
  <Button>
    <LanguageIcon />
    <span>Button</span>
  </Button>
  <Button variant="secondary">
    <LanguageIcon />
    <span>Button</span>
  </Button>
  <Button variant="tertiary">
    <LanguageIcon />
    <span>Button</span>
  </Button>
  <Button disabled>
    <LanguageIcon />
    <span>Button</span>
  </Button>
</Playground>

## Shape

Get same size on `width` and `height`, can have a 'circle' or 'square' `shape`.

<Playground>
  <Button shape="square" size="xs">
    <LanguageIcon size="xs" />
  </Button>
  <Button shape="circle" size="xs">
    <LanguageIcon size="xs" />
  </Button>
  <Button shape="square" size="sm">
    <LanguageIcon size="sm" />
  </Button>
  <Button shape="circle" size="sm">
    <LanguageIcon size="sm" />
  </Button>
  <Button shape="square">
    <LanguageIcon />
  </Button>
  <Button shape="circle">
    <LanguageIcon />
  </Button>
  <Button shape="square" size="lg">
    <LanguageIcon size="lg" />
  </Button>
  <Button shape="circle" size="lg">
    <LanguageIcon size="lg" />
  </Button>
  <Button shape="square" size="xl">
    <LanguageIcon size="xl" />
  </Button>
  <Button shape="circle" size="xl">
    <LanguageIcon size="xl" />
  </Button>
</Playground>

## Transform to a link

You can transform your Button component with `as` property to add you linker, `href` or `to`... all that you want.

<Playground>
  <Button as="a" href="http://www.welcometothejungle.co" target="_blank" rel="noopener nofollow">
    <SunIcon />
    <span>Welcome</span>
  </Button>
</Playground>

## Width

You can add `width` property from [xstyled](https://www.smooth-code.com/open-source/xstyled/docs/system-props/#layout).

<Playground>
  <Button width={1}>full width</Button>
  <Button width={1 / 2}>half width</Button>
</Playground>

## Radius

You can add `borderRadius` property from [xstyled](https://www.smooth-code.com/open-source/xstyled/docs/system-props/#borders).

<Playground>
  <Button>default</Button>
  <Button borderRadius="10px">10px</Button>
</Playground>

## ButtonGroup

Buttons grouped without space or radius.

<Playground>
  <ButtonGroup>
    <Button>First</Button>
    <Button>Second</Button>
    <Button>Third</Button>
    <Button>Fourth</Button>
  </ButtonGroup>
</Playground>

And you can add properties for all the button directly on ButtonGroup

<Playground>
  <ButtonGroup variant="secondary" size="xs">
    <Button>First</Button>
    <Button>Second</Button>
    <Button>Third</Button>
    <Button>Fourth</Button>
  </ButtonGroup>
</Playground>

## Properties

### Button

<Props of={Button} />

### ButtonGroup

<Props of={ButtonGroup} />
