---
name: Tag
route: /components/tag
menu: Components
---

import { Props, Playground } from 'docz'
import { Tag } from './index'
import { Code, Dependencies, TagVersion, Usage } from '../../docz'
const { dependencies, peerDependencies, component, name, version } = require('./package.json')

# Tag

<TagVersion version={version} name={name} />

## Install and import

<Usage component={component} name={name} peerDependencies={peerDependencies} />

## Variants

Admire these beautiful colors 💅

### Basics

<Playground>
  <Tag>Default</Tag>
  <Tag variant="primary">Primary</Tag>
  <Tag variant="secondary">Secondary</Tag>
  <Tag variant="dark">Dark</Tag>
</Playground>

### States

<Playground>
  <Tag variant="error">Error</Tag>
  <Tag variant="warning">Warning</Tag>
  <Tag variant="info">Info</Tag>
</Playground>

### Subs

<Playground>
  <Tag variant="blue">Blue</Tag>
  <Tag variant="green">Green</Tag>
  <Tag variant="orange">Orange</Tag>
  <Tag variant="pink">Pink</Tag>
  <Tag variant="purple">Purple</Tag>
  <Tag variant="red">Red</Tag>
  <Tag variant="turquoize">Turquoize</Tag>
  <Tag variant="yellow">Yellow</Tag>
</Playground>

## Sizes

Use size property with `sm` `md` or `lg`.

<Playground>
  <Tag size="sm" variant="secondary">
    Small
  </Tag>
  <Tag size="md" variant="secondary">
    Medium
  </Tag>
  <Tag size="lg" variant="secondary">
    Large
  </Tag>
</Playground>

## One character style

When we have only one character, we apply same size for the width and height (it implicitely sets `shape` prop to `square`)

<Playground>
  <Tag size="sm" variant="secondary">
    1
  </Tag>
  <Tag size="md" variant="secondary">
    1
  </Tag>
  <Tag size="lg" variant="secondary">
    1
  </Tag>
</Playground>

## Shape

Set `shape` prop to `square` to get a square no matter the content.

<Playground>
  <Tag size="lg" shape="square" variant="secondary">
    1
  </Tag>
  <Tag size="lg" shape="square" variant="secondary" width={50}>
    Long text
  </Tag>
</Playground>

Set `shape` prop to `circle` to set a border-radius `50%`.

<Playground>
  <Tag size="lg" shape="circle" variant="secondary">
    1
  </Tag>
</Playground>

## OnRemove

Simply add a function `onRemove` to reveal a cross icon with a remove action (or other).

<Playground>
  {() => {
    const [isHide, setHide] = useState(false)
    return isHide ? (
      <Text fontSize="body3" fontWeight="bold" color="danger.500" m={0}>
        Tag removed
      </Text>
    ) : (
      <Tag onRemove={() => setHide(true)}>Example of remove tag</Tag>
    )
  }}
</Playground>

## Properties

<Props of={Tag} />

## Dependencies

<Dependencies dependencies={dependencies} />

## Peer dependencies

<Dependencies dependencies={peerDependencies} />
