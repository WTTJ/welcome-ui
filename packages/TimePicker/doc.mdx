---
name: TimePicker
route: /fields/time-picker
menu: Fields
---

import { Props, Playground } from 'docz'
import { TimePicker } from './index.js'
import { Code, TagVersion } from '../../docz'
const { name, version } = require('./package.json')

# TimePicker

<TagVersion version={version} name={name} />

It is based on the [react-datepicker](https://github.com/Hacker0x01/react-datepicker) library.

_Note_: Pass a value of `null` if you don't want the default value of `Date.now()`.

## Import

<Code>{`import { TimePicker } from '@welcome-ui/time-picker'`}</Code>

## Simple use case

<Playground>
  <Form initialValues={{ welcome: Date.now() }}>
    <ConnectedField
      component={TimePicker}
      label="Time"
      name="welcome"
      onChange={date => console.log('date: ', date)}
    />
  </Form>
</Playground>

## Add an icon on the right

<Playground>
  <Form initialValues={{ welcome2: Date.now() }}>
    <ConnectedField
      component={TimePicker}
      icon={<DateIcon color="primary.500" />}
      iconPlacement="right"
      label="Time"
      name="welcome2"
    />
  </Form>
</Playground>

## Pass props to TimePicker

<Playground>
  <Form initialValues={{ welcome3: Date.now() }}>
    <ConnectedField
      name="welcome3"
      component={TimePicker}
      popperProps={{ zIndex: 50 }}
      timeIntervals={30}
    />
  </Form>
</Playground>

## Properties

<Props of={TimePicker} />
