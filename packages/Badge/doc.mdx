---
name: Badge
route: /components/badge
menu: Components
---

import { Playground, Props } from 'docz'
import { Code, Dependencies, TagVersion } from '../../docz'
const { dependencies, peerDependencies, name, version } = require('./package.json')

# Badge

<TagVersion version={version} name={name} />

Badge is the `<Tag />` component with small size. Check the doc of Tags component to see more examples.

## Import

<Code>{`import { Badge } from '@welcome-ui/badge'`}</Code>

## Examples

<Playground>
  <Badge>{0}</Badge>
  <Badge>{1}</Badge>
  <Badge>2</Badge>
  <Badge>3</Badge>
  <Badge>4</Badge>
  <Badge variant="primary">New</Badge>
</Playground>

## Properties

<Props of={Badge} />

## Dependencies

<Dependencies dependencies={dependencies} />

## Peer dependencies

<Dependencies dependencies={peerDependencies} />
