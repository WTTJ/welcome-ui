---
name: Toggle
route: /fields/toggle
menu: Fields
---

import { Props, Playground } from 'docz'
import { Toggle } from './index'
import { Checkbox } from '../Checkbox'
import { Code, Dependencies, TagVersion } from '../../docz'
const { dependencies, peerDependencies, name, version } = require('./package.json')

# Toggle

<TagVersion version={version} name={name} />

## Import

<Code>{`import { Toggle } from '@welcome-ui/toggle'`}</Code>

## Example

<Playground>
  <Form initialValues={{ swatch: true }}>
    <ConnectedField name="switch" component={Toggle} label="Switch example" mb="xl" />
    <ConnectedField
      name="swatch"
      component={Toggle}
      label="Disabled swatch example"
      disabled
      mb="xl"
    />
  </Form>
</Playground>

## Properties

<Props of={Checkbox} />

## Dependencies

<Dependencies dependencies={dependencies} />

## Peer dependencies

<Dependencies dependencies={peerDependencies} />
