---
name: Getting Started
route: /getting-started
---

import { Component } from 'react'
import { Props } from 'docz'
import { Code, Dependencies } from '../docz'
import { WuiWrapper } from './Core/WuiWrapper'
import { exampleHome } from '../docz/CodeExamples'
const { peerDependencies } = require('../package.json')

# Getting Started

## Installation

1. Install the **peer dependencies** listed below:

<Code language="bash" isCopyable>{`yarn add ${Object.keys(peerDependencies).join(' ')}`}</Code>

2. Install the the `core` component and any other components you need for your webapp e.g. if you need just a button…

<Code language="bash" isCopyable>{`yarn add @welcome-ui/core @welcome-ui/button`}</Code>

## Usage

Welcome UI provides an helper to help you setup your project: `<WuiWrapper />`.

<Code>{exampleHome}</Code>

## Properties

<Props of={WuiWrapper} />
