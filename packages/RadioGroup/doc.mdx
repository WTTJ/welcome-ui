---
name: Radios Group
route: /fields/radio-group
menu: Fields
---

import { Props, Playground } from 'docz'
import { ITEMS } from '../../scripts/tests'
import { RadioGroup } from './index'
import { Radio } from '../Radio'
import { Code, TagVersion } from '../../docz'
const { name, version } = require('./package.json')

# Radio Group

<TagVersion version={version} name={name} />

## Import

<Code>{`import { RadioGroup } from '@welcome-ui/radio-group'`}</Code>

## Example

<Playground>
  <Form
    initialValues={{}}
    validate={values => (!values.social ? { social: 'You must choose a social network' } : {})}
  >
    <ConnectedField
      component={RadioGroup}
      flexDirection="row"
      hint="Most popular is Twitter"
      label="Social networks"
      mb="xl"
      name="social"
      options={ITEMS}
      required
    />
    <Button type="submit">Submit</Button>
  </Form>
</Playground>

## Properties

<Props of={Radio} />
