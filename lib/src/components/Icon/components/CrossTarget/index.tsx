import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const CrossTargetIcon = (props: IconProps) => {
  return <Icon alt="CrossTarget" content={content} {...props} />
}
