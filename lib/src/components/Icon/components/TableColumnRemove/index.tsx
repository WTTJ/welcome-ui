import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const TableColumnRemoveIcon = (props: IconProps) => {
  return <Icon alt="TableColumnRemove" content={content} {...props} />
}
