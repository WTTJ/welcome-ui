import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const PhoneOutlineIcon = (props: IconProps) => {
  return <Icon alt="PhoneOutline" content={content} {...props} />
}
