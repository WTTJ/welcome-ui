import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const ArrowLineRightIcon = (props: IconProps) => {
  return <Icon alt="ArrowLineRight" content={content} {...props} />
}
