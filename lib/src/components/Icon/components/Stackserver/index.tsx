import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const StackserverIcon = (props: IconProps) => {
  return <Icon alt="Stackserver" content={content} {...props} />
}
