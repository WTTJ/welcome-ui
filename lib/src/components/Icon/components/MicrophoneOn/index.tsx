import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const MicrophoneOnIcon = (props: IconProps) => {
  return <Icon alt="MicrophoneOn" content={content} {...props} />
}
