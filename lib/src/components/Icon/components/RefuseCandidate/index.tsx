import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const RefuseCandidateIcon = (props: IconProps) => {
  return <Icon alt="RefuseCandidate" content={content} {...props} />
}
