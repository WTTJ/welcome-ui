import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const FlagPointedIcon = (props: IconProps) => {
  return <Icon alt="FlagPointed" content={content} {...props} />
}
