import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const JusticeHammerIcon = (props: IconProps) => {
  return <Icon alt="JusticeHammer" content={content} {...props} />
}
