import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const Heading5Icon = (props: IconProps) => {
  return <Icon alt="Heading5" content={content} {...props} />
}
