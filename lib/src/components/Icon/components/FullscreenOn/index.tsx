import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const FullscreenOnIcon = (props: IconProps) => {
  return <Icon alt="FullscreenOn" content={content} {...props} />
}
