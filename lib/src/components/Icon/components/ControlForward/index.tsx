import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const ControlForwardIcon = (props: IconProps) => {
  return <Icon alt="ControlForward" content={content} {...props} />
}
