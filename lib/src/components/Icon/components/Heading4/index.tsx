import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const Heading4Icon = (props: IconProps) => {
  return <Icon alt="Heading4" content={content} {...props} />
}
