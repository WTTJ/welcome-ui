import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const FlagOutlineIcon = (props: IconProps) => {
  return <Icon alt="FlagOutline" content={content} {...props} />
}
