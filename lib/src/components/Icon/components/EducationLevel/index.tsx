import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const EducationLevelIcon = (props: IconProps) => {
  return <Icon alt="EducationLevel" content={content} {...props} />
}
