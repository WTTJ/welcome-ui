import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const DocxIcon = (props: IconProps) => {
  return <Icon alt="Docx" content={content} {...props} />
}
