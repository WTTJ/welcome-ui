import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const Heading6Icon = (props: IconProps) => {
  return <Icon alt="Heading6" content={content} {...props} />
}
