import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const ControlBackwardIcon = (props: IconProps) => {
  return <Icon alt="ControlBackward" content={content} {...props} />
}
