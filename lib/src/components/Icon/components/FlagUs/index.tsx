import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const FlagUsIcon = (props: IconProps) => {
  return <Icon alt="FlagUs" content={content} {...props} />
}
