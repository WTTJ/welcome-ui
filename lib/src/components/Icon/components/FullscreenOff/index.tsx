import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const FullscreenOffIcon = (props: IconProps) => {
  return <Icon alt="FullscreenOff" content={content} {...props} />
}
