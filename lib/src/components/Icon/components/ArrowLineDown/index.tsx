import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const ArrowLineDownIcon = (props: IconProps) => {
  return <Icon alt="ArrowLineDown" content={content} {...props} />
}
