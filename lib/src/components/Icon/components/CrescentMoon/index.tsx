import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const CrescentMoonIcon = (props: IconProps) => {
  return <Icon alt="CrescentMoon" content={content} {...props} />
}
