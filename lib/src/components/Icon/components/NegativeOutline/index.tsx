import { Icon } from '../../Icon'
import type { IconProps } from '../../Icon.types'

import content from './content.json'

export const NegativeOutlineIcon = (props: IconProps) => {
  return <Icon alt="NegativeOutline" content={content} {...props} />
}
