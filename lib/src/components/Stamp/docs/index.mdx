---
category: icons-&-logo
description: The ImageStamp and IconStamp component displays an image or an icon with a background in different variant colors, such as "brand" or "warm". It adapts the appearance of the icon based on its current variants, ensuring that users receive clear visual feedback.
packageName: stamp
title: Stamp
isNew: true
---

### Variants

Use the `variant` prop to change the appearance of the tag. This prop doesn't work with ImageStamp.

<div data-playground="overview.tsx" data-component="Stamp"></div>

### Sizes

Use size property with `sm`, `md` (default) or `lg`.

<div data-playground="sizes.tsx" data-component="Stamp"></div>
