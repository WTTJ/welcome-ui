name: Demo link

on:
  pull_request:
    types: [opened]

jobs:
  add_demo_link:
    name: Add demo link to PR description
    runs-on: ubuntu-latest
    env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Update PR description
        if: ${{ !startsWith(github.event.pull_request.title, 'chore(deps-dev):') }}
        uses: actions/github-script@v6
        with:
          script: |
            const pullRequestId = context.payload.pull_request.number
            const pullRequestBranch = context.payload.pull_request.head.ref
            const pullRequestBody = context.payload.pull_request.body
            const pullRequestName = context.payload.pull_request.title
            const isDependenciesUpdate = title.startsWith('chore(deps-dev)')
            
            github.rest.pulls.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pullRequestId,
              body: `${pullRequestBody ? `${pullRequestBody}

              ---
              ` : ''}

             👀 [Visit Preview](https://${pullRequestBranch}--welcome-ui.netlify.app)` 
            });
