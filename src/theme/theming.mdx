---
name: Values
route: /theming/values
menu: Theming
---

import { Component } from 'react'
import { Playground } from 'docz'
import { getThemeValues } from '../../docz/themeValues'

# Values

<div>
  Here are all the possible values for your theme.
  <br /> These will be merged with the default theme.
</div>

## Listing

<Playground>
  {() => (
    <pre
      style={{
        fontFamily: 'monospace',
        fontSize: '14px'
      }}
    >
      {getThemeValues()}
    </pre>
  )}
</Playground>
