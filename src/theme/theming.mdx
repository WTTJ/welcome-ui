---
name: Values
route: /theming/values
menu: Theming
---

import { Component } from 'react'
import { Playground } from 'docz'
import { createTheme } from './core'

# Values

Here are all the possible values for your theme.<br /> These will be merged with the default theme.

<Playground>
  {() => {
    const theme = createTheme()

    const ThemeValues = () => (
      <>
        <pre style={{
          fontFamily: 'monospace',
          fontSize: '14px'
        }}>{JSON.stringify(theme, null, 2)}</pre>
      </>
    )

    return <ThemeValues />

}}

</Playground>
