---
name: Final Form
route: /usage-finalform
menu: Usage
---

import { Form } from 'react-final-form'
import { StyledPlayground as Playground } from '../docz.styled'
import { DoczCodeBlock } from '../docz/CodeBlock'

import { Button } from './components/Button'
import { Field } from './final/Field'
import { FieldGroup } from './components/FieldGroup'
import { RadioGroup } from './components/RadioGroup'

# Usage with form libraries

<Playground>
  {() => {
    const submitForm = (values) => {
      console.debug('values', values)
    }

    return (
      <Form
        initialValues={{ firstName: 'Fish', description: 'Fancy that', diet: 'herbivore', dogs: true, hungry: true, weekday: 'tuesday' }}
        onSubmit={submitForm}
        render={({ handleSubmit, values }) => (
          <form onSubmit={handleSubmit}>
            <Field name="firstName" type="text" placeholder="Boaty" label="First name" />
            <Field name="lastName" type="text" placeholder="McBoatface" label="Last name" />
            <RadioGroup label="Dietary">
              <Field name="diet" type="radio" value="vegan" label="Vegan" />
              <Field name="diet" type="radio" value="herbivore" label="Herbivore" />
              <Field name="diet" type="radio" value="omnivore" label="Omnivore" />
              <Field name="diet" type="radio" value="carnivore" label="Carnivore" />
            </RadioGroup>
            <RadioGroup label="Favourite weekday" flexDirection="row" required>
              <Field name="weekday" type="radioTab" value="monday" label="Monday" />
              <Field name="weekday" type="radioTab" value="tuesday" label="Tuesday" />
              <Field name="weekday" type="radioTab" value="wednesday" label="Wednesday" />
              <Field name="weekday" type="radioTab" value="thursday" label="Thursday" />
              <Field name="weekday" type="radioTab" value="friday" label="Friday" />
            </RadioGroup>
            <Field name="hungry" type="toggle" label="Hungry" />
            <FieldGroup label="Animals">
              <Field name="dogs" type="checkbox" label="Dogs" />
              <Field name="cats" type="checkbox" label="Cats" />
            </FieldGroup>
            <Field name="summary" type="textarea" placeholder="Keep it short" label="Summary" />
            <Field name="description" type="mde" placeholder="Keep it clean" label="Description" />
            <Field name="profile" type="fileupload" label="Profile pic" />
            <Button type="submit">Go!</Button>
            <DoczCodeBlock>{values}</DoczCodeBlock>
          </form>
        )}
      />
    )

}}

</Playground>
