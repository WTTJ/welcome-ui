---
name: Final Form
route: /usage-final-form
menu: Usage
---

import { Form } from 'react-final-form'
import { Playground } from '../docz.styled'
import { DoczCodeBlock } from '../docz/CodeBlock'

import { Button } from './components/Button'
import { ConnectedField } from './components/ConnectedField'
import { FieldGroup } from './components/FieldGroup'
import { RadioGroup } from './components/RadioGroup'

# Usage with form libraries

You'll probably use [Final Form](https://github.com/final-form/react-final-form), [Redux Form](https://github.com/erikras/redux-form) or [Formik](https://github.com/jaredpalmer/formik) to manage your form state. If so, make sure you import `Field` from `final/Field`.

<Playground>
  {() => {
    const submitForm = (values) => {
      console.debug('values', values)
    }

    return (
      <Form
        initialValues={{ firstName: 'Fish', description: 'Fancy that', diet: 'herbivore', dogs: true, hungry: true, weekday: 'tuesday' }}
        onSubmit={submitForm}
        render={({ handleSubmit, values }) => (
          <form onSubmit={handleSubmit}>
            <ConnectedField name="firstName" type="text" placeholder="Boaty" label="First name" />
            <ConnectedField name="lastName" type="text" placeholder="McBoatface" label="Last name" />
            <RadioGroup label="Dietary">
              <ConnectedField name="diet" type="radio" value="vegan" label="Vegan" />
              <ConnectedField name="diet" type="radio" value="herbivore" label="Herbivore" />
              <ConnectedField name="diet" type="radio" value="omnivore" label="Omnivore" />
              <ConnectedField name="diet" type="radio" value="carnivore" label="Carnivore" />
            </RadioGroup>
            <RadioGroup label="Favourite weekday" flexDirection="row" required>
              <ConnectedField name="weekday" type="radioTab" value="monday" label="Monday" />
              <ConnectedField name="weekday" type="radioTab" value="tuesday" label="Tuesday" />
              <ConnectedField name="weekday" type="radioTab" value="wednesday" label="Wednesday" />
              <ConnectedField name="weekday" type="radioTab" value="thursday" label="Thursday" />
              <ConnectedField name="weekday" type="radioTab" value="friday" label="Friday" />
            </RadioGroup>
            <ConnectedField name="hungry" type="toggle" label="Hungry" />
            <FieldGroup label="Animals">
              <ConnectedField name="dogs" type="checkbox" label="Dogs" />
              <ConnectedField name="cats" type="checkbox" label="Cats" />
            </FieldGroup>
            <ConnectedField name="summary" type="textarea" placeholder="Keep it short" label="Summary" />
            <ConnectedField name="description" type="mde" placeholder="Keep it clean" label="Description" />
            <ConnectedField name="profile" type="fileupload" label="Profile pic" />
            <Button type="submit">Go!</Button>
            <DoczCodeBlock>{values}</DoczCodeBlock>
          </form>
        )}
      />
    )

}}

</Playground>
