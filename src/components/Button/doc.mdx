---
name: Button
route: /components/button
menu: Components
---

import { Props, Link } from 'docz'
import { Playground } from '../../../docz.styled'

import { Box } from '../Box'
import { Button, IconButton } from './'
import { Icon } from '../Icon'

# Button

## Variants

Admire these beautiful variants 🌈

<Playground>
  <Box width="100%" fontSize="body3">
    Defaults
  </Box>
  <Button>primary (default)</Button>
  <Button variant="secondary">secondary</Button>
  <Button variant="tertiary">tertiary</Button>
  <Box width="100%" fontSize="body3" marginTop="10px">
    States
  </Box>
  <Button variant="primary-warning">primary-warning</Button>
  <Button variant="secondary-warning">secondary-warning</Button>
  <Button variant="primary-danger">primary-danger</Button>
  <Button variant="secondary-danger">secondary-danger</Button>
</Playground>

## Sizes

Use size property with `sm` `md`(default) or `lg`.

<Playground>
  <Button size="sm">Small</Button>
  <Button>Medium</Button>
  <Button size="lg">Large</Button>
</Playground>

## Disabled

All disabled buttons have the same style.

<Playground>
  <Button variant="primary-danger" disabled>
    Disabled
  </Button>
</Playground>

## With icons

<Playground>
  <Button>
    <Icon name="language" size="sm" />
    <span>Button</span>
  </Button>
  <Button variant="secondary">
    <Icon name="language" size="sm" />
    <span>Button</span>
  </Button>
  <Button variant="tertiary">
    <Icon name="language" size="sm" />
    <span>Button</span>
  </Button>
  <Button disabled>
    <Icon name="language" size="sm" />
    <span>Button</span>
  </Button>
</Playground>

## IconButton

Get same size on `width` and `height`, can be `rounded`.

<Playground flex>
  <IconButton size="sm">
    <Icon name="language" size="sm" />
  </IconButton>
  <IconButton size="sm" rounded>
    <Icon name="language" size="sm" />
  </IconButton>
  <IconButton>
    <Icon name="language" size="sm" />
  </IconButton>
  <IconButton rounded>
    <Icon name="language" size="sm" />
  </IconButton>
  <IconButton size="lg">
    <Icon name="language" size="md" />
  </IconButton>
  <IconButton size="lg" rounded>
    <Icon name="language" size="md" />
  </IconButton>
</Playground>

## Transform to a link

You can transform your Button component with `as` property to add you linker, `href` or `to`... all that you want.

<Playground flex>
  <IconButton
    as="a"
    href="http://www.welcometothejungle.co"
    target="_blank"
    rel="noopener nofollow"
  >
    <Icon name="external_link" size="sm" />
  </IconButton>
  <Button
    as="a"
    href="http://www.welcometothejungle.co"
    target="_blank"
    rel="noopener nofollow"
    variant="secondary"
  >
    <Icon name="sun" size="sm" />
    <span>Welcome</span>
  </Button>
  <Button as={Link} to="/" variant="tertiary">
    Homepage
  </Button>
</Playground>

## Width

You can add `width` property from [xstyled](https://www.smooth-code.com/open-source/xstyled/docs/system-props/#layout).

<Playground>
  <Button width={1}>full width</Button>
  <Button width={1 / 2}>half width</Button>
</Playground>

## Radius

You can add `borderRadius` property from [xstyled](https://www.smooth-code.com/open-source/xstyled/docs/system-props/#borders).

<Playground>
  <Button>default</Button>
  <Button borderRadius="10px">10px</Button>
</Playground>

## Properties

<Props of={Button} />
