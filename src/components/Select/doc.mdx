---
name: Select
route: /fields/select
menu: Fields
---

import { Props } from 'docz'
import { Playground } from '../../../docz/'

import { Select } from './index'

# Select

## Default

<Playground>
  {() => {
    const ITEMS = [
      { value: 'react', label: 'React' },
      { value: 'angular', label: 'Angular' },
      { value: 'vue', label: 'Vue' },
      { value: 'ember', label: 'Ember' },
      { value: 'elm', label: 'Elm' },
      { value: 'backbone', label: 'Backbone' }
    ]
    return <Select options={ITEMS} name="language" onChange={console.debug} required value="vue" />
  }}
</Playground>

## Disabled

<Playground>
  {() => {
    const ITEMS = [
      { value: 'react', label: 'React' },
      { value: 'angular', label: 'Angular' },
      { value: 'vue', label: 'Vue' },
      { value: 'ember', label: 'Ember' },
      { value: 'elm', label: 'Elm' },
      { value: 'backbone', label: 'Backbone' }
    ]
    return (
      <Select
        disabled
        options={ITEMS}
        name="language"
        onChange={console.debug}
        required
        value="vue"
      />
    )
  }}
</Playground>

## Properties

<Props of={Select} />
