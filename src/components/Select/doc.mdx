---
name: Select
route: /fields/input-tags
menu: Fields
---

import { Props } from 'docz'
import { Playground } from '../../../docz/'

import { Select } from './index'

# Select

## Default

<Playground>
  {() => {
    const ITEMS = [
      { value: 'angular', label: 'Angular' },
      { value: 'backbone', label: 'Backbone' },
      { value: 'elm', label: 'Elm' },
      { value: 'ember', label: 'Ember' },
      { value: 'react', label: 'React' },
      { value: 'vue', label: 'Vue' }
    ]
    return (
      <Select
        options={ITEMS}
        name="language"
        onChange={console.debug}
        required
        tags={[ITEMS[2]]}
      />
    )
  }}
</Playground>

## Disabled

<Playground>
  {() => {
    const ITEMS = [
      { value: 'angular', label: 'Angular' },
      { value: 'backbone', label: 'Backbone' },
      { value: 'elm', label: 'Elm' },
      { value: 'ember', label: 'Ember' },
      { value: 'react', label: 'React' },
      { value: 'vue', label: 'Vue' }
    ]
    return <Select disabled options={ITEMS} name="language" onChange={console.debug} required />
  }}
</Playground>

## Properties

<Props of={Select} />
