---
name: InputTags
route: /fields/input-tags
menu: Fields
---

import { Props } from 'docz'
import { Playground } from '../../../docz/'

import { InputTags } from './index'

# InputTags

## Default

<Playground>
  {() => {
    const ITEMS = [
      { value: 'angular', label: 'Angular' },
      { value: 'backbone', label: 'Backbone' },
      { value: 'elm', label: 'Elm' },
      { value: 'ember', label: 'Ember' },
      { value: 'react', label: 'React' },
      { value: 'vue', label: 'Vue' }
    ]
    return (
      <InputTags
        options={ITEMS}
        name="language"
        onChange={console.debug}
        required
        tags={[ITEMS[2]]}
      />
    )
  }}
</Playground>

## Disabled

<Playground>
  {() => {
    const ITEMS = [
      { value: 'angular', label: 'Angular' },
      { value: 'backbone', label: 'Backbone' },
      { value: 'elm', label: 'Elm' },
      { value: 'ember', label: 'Ember' },
      { value: 'react', label: 'React' },
      { value: 'vue', label: 'Vue' }
    ]
    return <InputTags disabled options={ITEMS} name="language" onChange={console.debug} required />
  }}
</Playground>

## Properties

<Props of={InputTags} />
