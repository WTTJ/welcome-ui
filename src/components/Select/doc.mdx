---
name: Select
route: /fields/select
menu: Fields
---

import { Props } from 'docz'
import { StyledPlayground as Playground } from '../../../docz.styled'

import { Select } from './index'

# Select

We use [react-select](https://github.com/JedWatson/react-select) and add our own custom styles from your theme.

## Example

<Playground>
  <Select
    name="description"
    options={[
      {
        label: 'Fish',
        value: 'fish'
      },
      {
        label: 'Cakes',
        value: 'cakes'
      },
      {
        label: 'Ahoy',
        value: 'ahoy'
      }
    ]}
    onChange={console.debug}
    placeholder="Placeholder"
    label="Select example"

/>

</Playground>

## Properties

<Props of={Select} />
