---
name: Radio buttons
route: /fields/radio-buttons
menu: Fields
---

import { Props } from 'docz'
import { Form, Playground } from '../../../docz/'
import { ITEMS } from '../../../helpers/tests'

import { ConnectedField } from '../ConnectedField'
import { RadioGroup } from '../RadioGroup'
import { RadioTab } from '../RadioTab'
import { Button } from '../Button'

import { InputRadio } from './index'

# Radio buttons

<Playground>
  <Form
    initialValues={{ social: 'behance' }}
    validate={values => (!values.social ? { social: 'Required' } : {})}
  >
    <ConnectedField
      component={RadioGroup}
      hint="Most popular is Twitter"
      label="Social networks"
      mb={0}
      name="social"
      options={ITEMS}
      required
    />
    <Button type="submit">Submit</Button>
  </Form>
</Playground>

## RadioTabs

<Playground>
  <Form initialValues={{ social_1: 'twitter', social_2: 'xing' }}>
    <>
      <ConnectedField
        component={RadioGroup}
        label="Social networks"
        mb="xl"
        name="social_1"
        options={ITEMS}
        required
        tabs
      />
      <ConnectedField
        component={RadioGroup}
        flexDirection="column"
        label="Social networks"
        mb="xl"
        name="social_2"
        options={ITEMS}
        required
        tabs
        width={1 / 3}
      />
    </>
  </Form>
</Playground>

## Properties

<Props of={InputRadio} />
