---
name: Icon
route: /components/icon
menu: Components
---

import { Props } from 'docz'
import { Playground, IconsList } from '../../../docz.styled'

import { Icon } from './index'

# Icon

## Size

Use size property with `xs`, `sm`, `md`, `lg` or `xl`.

<Playground>
  <Icon name="twitter" size="xs" />
  <Icon name="twitter" size="sm" />
  <Icon name="twitter" size="md" />
  <Icon name="twitter" size="lg" />
  <Icon name="twitter" size="xl" />
</Playground>

## Colour

Icons inherit the colour from their parent

<Playground>
  <div style={{ display: 'flex' }}>
    <div style={{ color: 'blue' }}>
      <Icon name="twitter" size="xl" />
    </div>
    <div style={{ color: 'orange' }}>
      <Icon name="twitter" size="xl" />
    </div>
    <div style={{ color: 'green' }}>
      <Icon name="twitter" size="xl" />
    </div>
  </div>
</Playground>

## Icons list

### Products

<Playground>
  {() => {
    const icons = ['ats', 'employer_branding', 'marketplace', 'welcome_home']

    return IconsList(icons)

}}

</Playground>

### Directions

<Playground>
  {() => {
    const icons = ['left', 'right', 'up', 'down']

    return IconsList(icons)

}}

</Playground>

### Actions

<Playground>
  {() => {
    const icons = ['add', 'archive', 'attachment', 'bulk', 'check', 'collapse', 'date', 'expand', 'cross', 'delete', 'external_link', 'filters', 'information', 'more', 'print', 'reset', 'refuse_candidate', 'search', 'settings']

    return IconsList(icons)

}}

</Playground>

### Appreciations

<Playground>
  {() => {
    const icons = ['negative_outline', 'positive_outline', 'negative', 'positive', 'negative_straigth', 'positive_straigth']

    return IconsList(icons)

}}

</Playground>

### Categories

<Playground>
  {() => {
    const icons = ['actions', 'cover_letter', 'details', 'resume', 'questions']

    return IconsList(icons)

}}

</Playground>

### Miscellaneous

<Playground>
  {() => {
    const icons = ['activity', 'address', 'avatar', 'candidate_status', 'comment_outline', 'comment', 'cross_target', 'department', 'email_outline', 'email', 'language', 'location', 'offer_status', 'origine', 'phone_outline', 'phone', 'promote', 'review_outline', 'review', 'source', 'special_pipeline', 'tag', 'female', 'male', 'create', 'update']

    return IconsList(icons)

}}

</Playground>

### WTF

<Playground>
  {() => {
    const icons = [
      'anniversary',
      'basketball',
      'birthday',
      'book',
      'bulb',
      'calcul',
      'camera',
      'celebrate',
      'chevron',
      'coffee',
      'compass',
      'computer',
      'crown',
      'dish',
      'flag',
      'gear',
      'handshake',
      'hard_hat',
      'hashtag',
      'heart',
      'lock',
      'map',
      'megaphone',
      'mic',
      'microship',
      'money',
      'mountain',
      'music',
      'paperplane',
      'pencil',
      'piechart',
      'pizza',
      'remote',
      'shield',
      'spanner',
      'stackserver',
      'sun',
      'target',
      'thumbup',
      'tree',
    ]

    return IconsList(icons)

}}

</Playground>

### Social networks

<Playground>
  {() => {
    const icons = ['behance', 'dribbble', 'facebook', 'github', 'instagram', 'linkedin', 'stackoverflow', 'twitter', 'xing', 'youtube']

    return IconsList(icons)

}}

</Playground>

## Properties

<Props of={Icon} />
