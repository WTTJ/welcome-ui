---
name: Growl
route: /components/growl
menu: Components
---

import { Props } from 'docz'
import { Playground } from '../../../docz.styled'

import { Box } from '../Box'
import { Button } from '../Button'
import { Growl, GrowlAction, GrowlClose, GrowlTitle, GrowlContainer, useGrowl } from './'
import { Icon } from '../Icon'

# Growl

Customize with `<GrowlTitle>`, `<GrowlAction>` or `<GrowlClose>`.

## Example

To use `Growl` components you'll want to…

1. Import and spread the `useGrowl` hook
2. Add a `GrowlContainer` to your app
3. Add a trigger to add `Growl` items to the `GrowlContainer`

To test the placement of `Growl` items, open the editor and change the `position` prop of the `GrowlContainer`. 

<Playground>
  {() => {
    // Import and spread the `useGrowl` hook
    const { addGrowl, removeGrowl, content } = useGrowl() 

    // Create a `Growl` item, passing `removeGrowl` through as `onClose`
    const growl = (
      <Growl onClose={removeGrowl}> 
        <GrowlTitle>
          <Icon name="megaphone" size="sm" /> Lorem ipsum dolor sit amet
        </GrowlTitle>
        Class aptent taciti sociosqu ad litora torquent per conubia nostra, per inceptos himenaeos
        <GrowlAction>
          <Button size="sm" variant="tertiary" onClick={console.debug}>
            Action button
          </Button>
        </GrowlAction>
      </Growl>
    )

    // Create a trigger (here we use a button)
    return (
      <>
        <GrowlContainer content={content} position='top-left' />
        <Button onClick={() => addGrowl(growl)}>Show Growl</Button>
      </>
    )

}}

</Playground>

## Variants

Variant properties `info` (default), `error` or `warning` with automatic color.

<Playground>
  <Box mb="10px">
    <Growl onClose={console.debug}>
      <GrowlTitle variant="error">
        <Icon name="flag" size="sm" /> Error growl
      </GrowlTitle>
      Class aptent taciti sociosqu ad litora torquent per conubia nostra, per inceptos himenaeos
      <GrowlAction>
        <Button size="sm" variant="secondary-danger">
          Action button
        </Button>
      </GrowlAction>
    </Growl>
  </Box>
  <Box mb="10px">
    <Growl onClose={console.debug}>
      <GrowlTitle variant="warning">
        <Icon name="shield" size="sm" /> Warning growl
      </GrowlTitle>
      Class aptent taciti sociosqu ad litora torquent per conubia nostra, per inceptos himenaeos
      <GrowlAction>
        <Button size="sm" variant="secondary-warning">
          Action button
        </Button>
      </GrowlAction>
    </Growl>
  </Box>
  <Box>
    <Growl onClose={console.debug}>
      <GrowlTitle>
        <Icon name="megaphone" size="sm" /> Info growl (default)
      </GrowlTitle>
      Class aptent taciti sociosqu ad litora torquent per conubia nostra, per inceptos himenaeos
      <GrowlAction>
        <Button size="sm" variant="secondary">
          Action button
        </Button>
      </GrowlAction>
    </Growl>
  </Box>
</Playground>

## Custom elements

Change close component, don't add icon on title, remove action button...

<Playground>
  <Box mb="10px">
    <Growl>
      <GrowlTitle>Without icon on title, action button and close button</GrowlTitle>
      Class aptent taciti sociosqu ad litora torquent per conubia nostra, per inceptos himenaeos
    </Growl>
  </Box>
  <Box mb="10px">
    <Growl
      onClose={console.debug}
      close={
        <GrowlClose>
          <Icon name="positive" size="sm" />
        </GrowlClose>
      }
    >
      <GrowlTitle>Custom close button with GrowlClose component</GrowlTitle>
      Class aptent taciti sociosqu ad litora torquent per conubia nostra, per inceptos himenaeos
    </Growl>
  </Box>
  <Box>
    <Growl onClose={console.debug} close={<Icon name="positive" size="md" />}>
      <GrowlTitle>Custom close button</GrowlTitle>
      Class aptent taciti sociosqu ad litora torquent per conubia nostra, per inceptos himenaeos
    </Growl>
  </Box>
</Playground>

## Properties

### GrowlContainer

<Props of={GrowlContainer} />

### Growl

<Props of={Growl} />

### GrowlTitle

<Props of={GrowlTitle} />
