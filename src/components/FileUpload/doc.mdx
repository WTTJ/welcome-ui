---
name: FileUpload
route: /fields/file-upload
menu: Fields
---

import { Props } from 'docz'
import { Playground } from '../../../docz/'
import { useState } from 'react'

import { FileUpload } from './index'
import { Button } from '../Button'
import { Icon } from '../Icon'

# FileUpload

FileUpload uses render props so you can pass a button through. You have access to `openFile` to open the native file chooser dialog box.

## Default

<Playground>
  {() => {
    const [file, setFile] = useState(null)
    const handleChange = e => {
      setFile(e.target.value)
    }
    return (
      <FileUpload
        accept="image/*"
        name="default"
        label="Avatar"
        hint="Files must be <200MB"
        required
        onChange={handleChange}
        onAddFile={console.debug}
        onRemoveFile={console.debug}
        onError={console.error}
        value={file}
      >
        {({ openFile }) => <Button onClick={openFile}>Upload avatar</Button>}
      </FileUpload>
    )
  }}
</Playground>

## Multiple

<Playground>
  {() => {
    const [file, setFile] = useState(null)
    const handleChange = e => {
      setFile(e.target.value)
    }
    return (
      <FileUpload
        accept="application/pdf"
        name="multiple"
        label="Avatar"
        hint="Files must be <200MB"
        multiple
        onChange={handleChange}
        onAddFile={console.debug}
        onRemoveFile={console.debug}
        onError={console.error}
        required
        value={file}
      >
        {({ openFile }) => (
          <Button onClick={openFile} variant="secondary">
            Choose file(s)
          </Button>
        )}
      </FileUpload>
    )
  }}
</Playground>

## Show preview if value exists

<Playground>
  {() => {
    const DefaultView = ({ openFile }) => (
      <>
        <h3>Add picture</h3>
        <p>Drag and drop a file here or…</p>
        <Button onClick={openFile} type="button">
          Choose file
        </Button>
      </>
    )
    return (
      <FileUpload
        name="preview"
        label="Avatar"
        hint="Files must be <200MB"
        required
        onChange={console.debug}
        onAddFile={console.debug}
        onRemoveFile={console.debug}
        onError={console.error}
        value="https://cdn-development.welcometothejungle.co/uploads/robert/applicants/303416437e/avatars/0.jpg?v=63731713698"
      >
        {({ openFile }) => <Button onClick={openFile}>Upload file</Button>}
      </FileUpload>
    )
  }}
</Playground>

## Disabled

<Playground>
  <FileUpload disabled name="disabled">
    {({ openFile }) => (
      <Button disabled onClick={openFile}>
        Choose file
      </Button>
    )}
  </FileUpload>
</Playground>

## Properties

<Props of={FileUpload} />
