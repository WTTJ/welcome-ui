---
name: 1. Intro
route: /fields/intro
menu: Fields
---

import { Props } from 'docz'
import { DoczForm, Playground } from '../../../docz/'

import { ConnectedField } from '../ConnectedField'
import { InputText } from '../InputText'

# Field

We recommend using [Final Form](https://github.com/final-form/react-final-form), [Redux Form](https://github.com/erikras/redux-form) or [Formik](https://github.com/jaredpalmer/formik) with these components. If so, use `ConnectedField` for your fields (as in the examples below).

The examples below give you an overview of common props (e.g. `size`, `disable`, `required` etc.)

You can find props for each field component in the other `Fields` pages.

## Sizes

<Playground>
  <DoczForm initialValues={{}}>
    <>
      <ConnectedField
        name="small"
        placeholder="Placeholder"
        component={InputText}
        label="I'm a small sized field"
        size="sm"
        mb="xl"
      />
      <ConnectedField
        name="medium"
        placeholder="Placeholder"
        component={InputText}
        label="I'm a medium sized field"
        size="md"
        mb="xl"
      />
      <ConnectedField
        name="large"
        placeholder="Placeholder"
        component={InputText}
        label="I'm a large sized field which is my default size"
        mb="xl"
      />
    </>
  </DoczForm>
</Playground>

## Variants

<Playground>
  <DoczForm initialValues={{}}>
    <>
      <ConnectedField
        name="default"
        placeholder="Placeholder"
        component={InputText}
        label="Label"
        hint="A hint"
        mb="xl"
      />
      <ConnectedField
        name="warning"
        placeholder="Placeholder"
        component={InputText}
        label="Label"
        warning="A warning"
        mb="xl"
      />
      <ConnectedField
        name="error"
        placeholder="Placeholder"
        component={InputText}
        label="Label"
        error="An error"
        mb="xl"
      />
    </>
  </DoczForm>
</Playground>

## Disabled

<Playground>
  <DoczForm initialValues={{ disabled: 'Disabled value' }}>
    <ConnectedField
      name="disabled"
      placeholder="Placeholder"
      component={InputText}
      label="Label"
      disabled
    />
  </DoczForm>
</Playground>

## Required

<Playground>
  <DoczForm initialValues={{}}>
    <ConnectedField
      name="required"
      placeholder="Placeholder"
      component={InputText}
      label="Label"
      required
    />
  </DoczForm>
</Playground>

## Properties

<Props of={ConnectedField} />
