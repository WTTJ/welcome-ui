---
name: InputText
route: /fields/input-text
menu: Fields
---

import { Props } from 'docz'
import { Playground } from '../../../docz/Playground'
import { DoczForm } from '../../../docz/Form'

import { ConnectedField } from '../ConnectedField'
import { Icon } from '../Icon'
import { InputText } from './index'

# InputText

## Default

<Playground>
  <DoczForm initialValues={{}}>
    <ConnectedField
      name="firstName"
      component={InputText}
      placeholder="Boaty"
      label="First name"
      required
    />
  </DoczForm>
</Playground>

## Clearable

Use the `isClearable` prop to add a cross button to remove the content (value) of the `TextInput`

<Playground>
  <DoczForm initialValues={{ firstName: 'Trevor' }}>
    <ConnectedField
      component={InputText}
      isClearable
      label="First name"
      name="firstName"
      placeholder="Boaty"
    />
  </DoczForm>
</Playground>

## Icon

Pass an `icon` through to decorate your `InputText`

<Playground>
  <DoczForm initialValues={{}}>
    <ConnectedField
      component={InputText}
      icon={<Icon name="avatar" color="nude.500" />}
      label="First name"
      name="firstName"
      placeholder="Boaty"
    />
  </DoczForm>
</Playground>

## Sizes

<Playground>
  <DoczForm initialValues={{}}>
    <>
      <ConnectedField
        component={InputText}
        label="I'm a small sized field"
        mb="xl"
        name="small"
        placeholder="Placeholder"
        size="sm"
      />
      <ConnectedField
        component={InputText}
        label="I'm a medium sized field"
        mb="xl"
        name="medium"
        placeholder="Placeholder"
        size="md"
      />
      <ConnectedField
        component={InputText}
        label="I'm a large sized field which is my default size"
        mb="xl"
        name="large"
        placeholder="Placeholder"
      />
    </>
  </DoczForm>
</Playground>

## Inline

<Playground>
  <DoczForm initialValues={{ firstName: 'Trevor' }}>
    <ConnectedField
      component={InputText}
      disabled
      flexDirection="row"
      label="First name"
      name="firstName"
      placeholder="Boaty"
      required
    />
  </DoczForm>
</Playground>

## Disabled

<Playground>
  <DoczForm initialValues={{ firstName: 'Trevor' }}>
    <ConnectedField
      component={InputText}
      disabled
      label="First name"
      name="firstName"
      placeholder="Boaty"
      required
    />
  </DoczForm>
</Playground>

## Properties

<Props of={InputText} />
