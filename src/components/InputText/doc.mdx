---
name: InputText
route: /fields/input-text
menu: Fields
---

import { Props } from 'docz'
import { Playground } from '../../../docz/Playground'
import { getFormValues, TestFinalForm } from '../../../docz/FinalForm'

import { ConnectedField } from '../ConnectedField'
import { Icon } from '../Icon'
import { InputText } from './index'

# InputText

## Default

<Playground>
  <TestFinalForm initialValues={{ firstName: 'Trevor' }}>
    <ConnectedField
      name="firstName"
      component={InputText}
      placeholder="Boaty"
      label="First name"
      required
    />
  </TestFinalForm>
</Playground>

## Clearable

Use the `isClearable` prop to add a cross button to remove the content (value) of the `TextInput`

<Playground>
  <TestFinalForm initialValues={{ firstName: 'Trevor' }}>
    <ConnectedField
      component={InputText}
      isClearable
      label="First name"
      name="firstName"
      placeholder="Boaty"
    />
  </TestFinalForm>
</Playground>

## Icon

Pass an `icon` through to decorate your `InputText`

<Playground>
  <TestFinalForm initialValues={{}}>
    <ConnectedField
      component={InputText}
      icon={<Icon name="avatar" color="nude.500" />}
      label="First name"
      name="firstName"
      placeholder="Boaty"
    />
  </TestFinalForm>
</Playground>

## Sizes

<Playground>
  <TestFinalForm initialValues={{ small: 'I am small' }}>
    <>
      <ConnectedField
        component={InputText}
        label="I'm a small sized field"
        mb="xl"
        name="small"
        placeholder="Placeholder"
        size="sm"
      />
      <ConnectedField
        component={InputText}
        label="I'm a medium sized field"
        mb="xl"
        name="medium"
        placeholder="Placeholder"
        size="md"
      />
      <ConnectedField
        component={InputText}
        label="I'm a large sized field which is my default size"
        mb="xl"
        name="large"
        placeholder="Placeholder"
      />
    </>
  </TestFinalForm>
</Playground>

## Disabled

<Playground>
  <TestFinalForm initialValues={{ firstName: 'Trevor' }}>
    <ConnectedField
      component={InputText}
      disabled
      label="First name"
      name="firstName"
      placeholder="Boaty"
      required
    />
  </TestFinalForm>
</Playground>

## Properties

<Props of={InputText} />
