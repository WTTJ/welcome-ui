---
name: Badge
route: /components/badge
menu: Components
---

import { Link, Props } from 'docz'
import { Playground } from '../../../docz/'
import { Badge } from './index'

# Badge

Badge is the `<Tag />` component with small size. Check the doc of <Link to="/components/tag">Tags</Link> component to see more examples.

### Examples

<Playground>
  <Badge>{0}</Badge>
  <Badge>{1}</Badge>
  <Badge>2</Badge>
  <Badge>3</Badge>
  <Badge>4</Badge>
  <Badge variant="primary">New</Badge>
</Playground>
