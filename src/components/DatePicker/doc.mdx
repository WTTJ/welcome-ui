---
name: DatePicker
route: /fields/date-picker
menu: Fields
---

import fr from 'date-fns/locale/fr'
import { Props } from 'docz'
import { Playground } from '../../../docz/'
import { Form } from '../../../docz/Form'

import { ConnectedField } from '../ConnectedField'
import { Icon } from '../Icon'
import { DatePicker } from './index.js'

# DatePicker

It is based on the [react-datepicker](https://github.com/Hacker0x01/react-datepicker) library.

_Note_: Pass a value of `null` if you don't want the default value of `Date.now()`.

## Simple use case

<Playground>
  <Form initialValues={{ welcome: Date.now() }}>
    <ConnectedField component={DatePicker} label="Date" name="welcome" />
  </Form>
</Playground>

## Pass props from [the original library](https://github.com/Hacker0x01/react-datepicker).

<Playground>
  <Form initialValues={{ welcome: Date.now() }}>
    <ConnectedField
      component={DatePicker}
      dateFormat="MMM dd yyyy"
      label="Date"
      maxDate={new Date()}
      name="welcome"
      yearDropdownItemNumber={30}
    />
  </Form>
</Playground>

## Give an Icon

<Playground>
  <Form initialValues={{ welcome: Date.now() }}>
    <ConnectedField
      component={DatePicker}
      icon={<Icon name="date" color="nude.500" />}
      label="Date"
      name="welcome"
    />
  </Form>
</Playground>

## fr locale

<Playground>
  <Form initialValues={{}}>
    <ConnectedField component={DatePicker} label="Date" locale={fr} name="welcome" />
  </Form>
</Playground>

## Properties

<Props of={DatePicker} />
